{
  "directories": {
    "lib": "../lib",
    "test": "../test",
    "example": "../examples"
  },
  "scripts": {
    "compile": "./compile",
    "clean": "./clean"
  },
  "dependencies": {
    "minify": "^5.1.0"
  }
}
